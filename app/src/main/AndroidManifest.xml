<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.university">

    <application
        android:allowBackup="true"
        android:icon="@drawable/graduated"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UniversityDescription" android:theme="@style/UniversityDescriotion">

        </activity>
        <activity
            android:name=".RecyclerUniversity"
            android:theme="@style/RecylerUniversity">

        </activity>
        <activity
            android:name=".SignupActivity"
            android:theme="@style/sigunp" />
        <activity android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>